name: Apply audit UX hardening

on:
  push:
    branches: [audit-hardening-work]
    paths:
      - '.github/workflows/apply-audit-ux-hardening.yml'
      - 'scripts/apply-audit-ux-hardening.py'
      - 'scripts/repair-audit-ux-script.py'

permissions:
  contents: write

jobs:
  harden:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: python scripts/repair-audit-ux-script.py
      - run: python scripts/apply-audit-ux-hardening.py
      - run: npm run typecheck
      - run: npm run lint -- --max-warnings=0
      - run: npm test
      - run: npm run build
      - run: git diff --check
      - name: Commit validated fixes
        shell: bash
        run: |
          rm .github/workflows/apply-audit-ux-hardening.yml
          rm scripts/apply-audit-ux-hardening.py
          rm scripts/repair-audit-ux-script.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: harden recovery and game presentation"
          git push origin HEAD:audit-hardening-work
